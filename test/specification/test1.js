import { assert } from 'chai';
import { x } from '../../dist/out.js';

describe('Array', () => {
  describe('#indexOf()', () => {
    it('should return -1 when the value is not present', () => {
      console.log(document, document.createElement('div'), x);
      assert.typeOf('foo', 'string');
    });
  });
});
